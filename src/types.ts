export type UserData = {
  uid: string;
  displayName: string;
  email: string;
  photoURL: string;
};
export type UserForm = {
  username: string;
  email: string;
  password: string;
  confirmPassword: string;
};
